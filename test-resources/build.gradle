project(':test-resources:parseCases') {
  sourceSets {
      main.kotlin.srcDirs = ['kotlin']
      main.java.srcDirs = ['java']
  }
}

project(':test-resources:baseClasses') {
  sourceSets {
      main.kotlin.srcDirs = ['kotlin']
      main.java.srcDirs = ['java']
  }
}

project(':test-resources:renameBaseClasses') {
  sourceSets {
      main.kotlin.srcDirs = ['kotlin']
      main.java.srcDirs = ['java']
  }
}

project(':test-resources:addRmRenameClasses') {
  sourceSets {
      main.kotlin.srcDirs = ['kotlin']
      main.java.srcDirs = ['java']
  }
}
